return {
    MESSAGE_PROCESS_INTERVAL = 500,
    MESSAGE_PROCESS_MAX_RETRY_COUNT = 10,
    FLYMODE_INTERVAL = 1000 * 60 * 60 * 24,
    HEART_BEAT_INTERVAL = 1000 * 60 * 5,
    ENABLE_HEART_BEAT = true,
}
